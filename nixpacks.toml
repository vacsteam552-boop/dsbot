[phases.setup]
nixPkgs = ["python312", "chromium", "chromedriver"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.start]
cmds = ["python main.py"]

[phases.deploy]
cmds = ["python main.py"]